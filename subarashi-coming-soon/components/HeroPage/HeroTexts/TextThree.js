import { Typography, Box } from "@mui/material";
import { fontStyles } from "../../../utils/fontStyles";

export default function TextThree() {
  return (
    <Box>
      <Typography style={fontStyles.largeBody}>Is on the way.</Typography>
    </Box>
  );
}
